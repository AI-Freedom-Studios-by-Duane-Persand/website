/**
 * Infrastructure Adapters Exports
 * 
 * Barrel export for infrastructure adapters:
 * - IContentGenerator implementations
 * - IStorageProvider implementations
 * - ISocialPublisher implementations (future)
 */

export { PoeContentGeneratorAdapter } from './poe-content-generator.adapter';
export { R2StorageAdapter } from './r2-storage.adapter';
